use crate::ast::{AstNode, FunctionDeclNode};

fn resgister_array_methods(fns: &mut Vec<Box<dyn AstNode>>) {
    let push_method = r"#
         
    #";
}

pub fn register_primitive_methods(fns: &mut Vec<Box<dyn AstNode>>) {}
